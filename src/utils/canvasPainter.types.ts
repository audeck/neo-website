export type ShootingStar = {
    x: number,
    y: number,
    dx: number,
    dy: number,
    r: number,
    intervalId: NodeJS.Timer, 
}

export type Star = {
    cx: number,
    cy: number,
    rotation: number,
    scale: number,
}

